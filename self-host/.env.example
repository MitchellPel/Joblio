# Copy to .env and change the password before first start.
#   copy .env.example .env

POSTGRES_USER=joblio
POSTGRES_PASSWORD=change-me-to-a-long-password
POSTGRES_DB=joblio

# Host ports (office LAN)
POSTGRES_PORT=5432
API_PORT=8080
